# HTML

Raw HTML output. Renders full-width (no label column). Content is passed through `wp_kses_post()`.

```php
'promo_banner' => [
    'type'    => 'html',
    'content' => '<div class="my-banner"><p>Upgrade to Pro for advanced features.</p></div>',
],
```
